<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yilena.myShortLink.project.dao.ShortLinkMapper">


    <select id="pageShortLink"
            parameterType="org.yilena.myShortLink.project.entry.DTO.request.ShortLinkPageReqDTO"
            resultType="org.yilena.myShortLink.project.entry.DO.ShortLinkDO">
    select *
    from t_link
    <where>
        <if test="gid != null">
            and gid = #{gid}
        </if>
    </where>
    and del_flag = 0
    and enable_status = 0
    order by update_time desc
    </select>
    <select id="listGroupShortLinkCount" resultType="java.util.Map">
        select gid, count(1) as count
        from t_link
        where gid in
              <foreach collection="requestParam" item="gid" open="(" separator="," close=")">
                 #{gid}
              </foreach>
        and del_flag = 0
        and enable_status = 0
        group by gid
    </select>
    <select id="listIdsByGid" resultType="java.lang.Long">
        SELECT id
        FROM t_link
        WHERE gid = #{gid}
          AND del_flag = 0
          AND enable_status = 1
        ORDER BY create_time DESC, id DESC
        LIMIT #{offset}, #{size}
    </select>
    <select id="listByIds" resultType="org.yilena.myShortLink.project.entry.DO.ShortLinkDO">
        SELECT *
        FROM t_link
        WHERE id IN
              <foreach collection="ids" item="id" open="(" separator="," close=")">
                  #{id}
              </foreach>
          AND gid = #{gid}
          AND del_flag = 0
          AND enable_status = 1
    </select>
    <select id="countByGid" resultType="java.lang.Long">
        SELECT count(1)
        FROM t_link
        WHERE gid = #{gid}
          AND del_flag = 0
          AND enable_status = 1
    </select>
</mapper>